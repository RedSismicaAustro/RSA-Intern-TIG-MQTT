# ==============================================================================
# Ejemplo de configuración de variables de entorno
# Docker Compose Unificado - TIG Stack
# ==============================================================================
#
# IMPORTANTE:
# 1. Copiar este archivo a .env: cp .env.example .env
# 2. Editar .env con tus credenciales reales
# 3. NO commitear el archivo .env (está en .gitignore)

# ==============================================================================
# Host Paths Configuration
# ==============================================================================
DATA_DIR=/home/rsa/data
DRIVE_DIR=/home/rsa/datos_estaciones_drive

# ==============================================================================
# MQTT Broker Configuration
# ==============================================================================
MQTT_BROKER=broker.emqx.io
MQTT_USERNAME=rsa_user
MQTT_PASSWORD=change_this_password

# ==============================================================================
# InfluxDB Configuration
# ==============================================================================
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=admin_secure_password_123
INFLUXDB_ORG=rsa
INFLUXDB_BUCKET=telemetry
INFLUXDB_EVENTS_BUCKET=rsa_events
INFLUXDB_RETENTION=90d

# Configuración de Telegraf
TELEGRAF_CLIENT_ID=telegraf-events-oficina

# Token de autenticación para InfluxDB v2
# IMPORTANTE: Genera un token seguro con: openssl rand -hex 32
# Este token debe ser el mismo que uses en Telegraf y Grafana
INFLUXDB_TOKEN=my-super-secret-auth-token-change-me

# ==============================================================================
# Grafana Configuration
# ==============================================================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=grafana_secure_password_123

# ==============================================================================
# Notas:
# ==============================================================================
#
# - MQTT_BROKER: Dirección IP o hostname del broker MQTT
#   Ejemplos:
#     - Broker local: localhost o 127.0.0.1
#     - Broker RSA: 192.168.1.100
#     - Broker público: broker.emqx.io
#
# - INFLUXDB_RETENTION: Política de retención de datos
#   Ejemplos: 7d, 30d, 90d, 365d, 0 (infinito)
#
# - INFLUXDB_TOKEN: Token de autenticación para API de InfluxDB
#   Se recomienda generar uno aleatorio y seguro
#
# - Todas las contraseñas deben ser diferentes y seguras en producción
